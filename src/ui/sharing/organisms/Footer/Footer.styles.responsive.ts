import { css } from "@emotion/css";
import { forsize } from "@mixins";

export const footer = forsize({
  "desktop-mid": css``,
  desktop: css``,
});

export const container = forsize({
  "desktop-mid": css``,
  desktop: css``,
});

export const list = forsize({
  "desktop-mid": css``,
  desktop: css``,
  "phone-xbig": css`
    flex-direction: column;
    align-items: flex-end;
  `,
});

export const item = forsize({
  "desktop-mid": css``,
  desktop: css``,
});

export const link = forsize({
  "desktop-mid": css``,
  desktop: css``,
});

export const icon = forsize({
  "desktop-mid": css``,
  desktop: css``,
});
